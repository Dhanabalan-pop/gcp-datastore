indexes:
  - kind: Student
    properties:
      - name: student_id
        direction: asc
      - name: name
        direction: asc
      - name: participation_status
        direction: asc
      - name: performance_category
        direction: asc

  - kind: Student
    properties:
      - name: registration_date
        direction: desc
      - name: participation_status
        direction: asc
